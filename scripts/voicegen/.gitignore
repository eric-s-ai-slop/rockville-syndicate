# Derived work list — regenerated by extract-lines.ts
lines.json

# Python venv if created here
.venv-tts/

# Keep refs/ (reference wavs) committed for reproducible regeneration,
# but ignore scratch/raw recordings.
refs/*.raw
refs/*_scratch.wav
